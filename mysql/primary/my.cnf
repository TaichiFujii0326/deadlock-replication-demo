[mysqld]
server-id                = 1
log-bin                  = mysql-bin
binlog-format            = ROW
gtid-mode                = ON
enforce-gtid-consistency = ON
log-replica-updates      = ON

# Make flush pressure visible under contention
innodb-log-buffer-size   = 1M
